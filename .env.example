# Leave empty for same-origin /predict (Vite dev proxy or nginx in production).
# Set only if the API runs on a different origin, e.g. VITE_API_URL=http://localhost:8000
VITE_API_URL=
CORS_ORIGINS=http://localhost:5173
